# Supabase 연결값 (없으면 더미 데이터로 동작)
#
# Supabase 대시보드 → Project Settings → API 에서 값만 복사해 채우세요.
# (이름은 Supabase가 보여주는 그대로 사용합니다)

NEXT_PUBLIC_SUPABASE_URL=

# publishable key (= 공개키, 프론트 노출 OK)
# ※ secret key / service_role 키는 여기에 넣지 말 것
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
